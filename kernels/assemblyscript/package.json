{
  "name": "@cloudflare-auth-hasher/assemblyscript-kernel",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "asc assembly/index.ts --target release --outFile build/kernel.wasm",
    "check": "npm run build"
  },
  "devDependencies": {
    "assemblyscript": "^0.28.9"
  }
}
